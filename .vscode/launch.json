{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch App in Chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/src",
            // This ensures the Backend and Frontend terminals start before Chrome opens
            "preLaunchTask": "Automated Full Stack Launch"
        }
    ]
}